# POC fix and verification checkpoint

The POC is materially stronger after these fixes, but core acceptance remains incomplete. No additional agents were started or resumed in this continuation.

## Verified results

Current full run `run_20260910_120652_943c332e` reports 23 core scenarios passing, zero failing, three incomplete, and five optional adapter skips. The process correctly returned nonzero because incomplete core checks block acceptance. All 32 focused regression tests passed. The implementation snapshot was unchanged during the scenario run. See [run report](evidence/run_20260910_120652_943c332e/report.md), [assertions](evidence/run_20260910_120652_943c332e/assertions.json), and [manifest](evidence/run_20260910_120652_943c332e/manifest.json).

This is scenario-run evidence, not proof that every normative requirement is implemented. In particular, a passing scenario with limited assertions must not substitute for the full acceptance contract.

## Fixes verified in this continuation

- Activated fixed-destination, loopback-published ingress while preserving internal-only networks for ordinary core services. Actual external TCP denial and local business traffic pass.
- Reconciled stale approved service/credential records using authenticated intake and provisioning, preserving existing data and history. Approval now updates the registered target and category. Provisioning rejects targets outside its fixed local profile. Added explicit bootstrap `--seed-only --reconcile` mode.
- Corrected the registered caller identity to `spiffe://lab.local/workload/caller`. Identity issuer blocking and fresh/existing target connection denial pass.
- Added persistent restricted candidate storage. No raw candidate goes into the registry.
- Fixed dynamic session containment when Vault drops a login role but its connections survive. Capture backend PID and start time before revocation; terminate and observe those same backends after revocation. Missing observation permission remains an error. Added the PostgreSQL statistics-read grant needed by the containment controller and three regression tests.
- Expanded metrics verification to schedule two controlled attempts, complete one, leave one missing, remove ownership, and confirm the denominator, unresolved ownership, and open duration remain visible. The test restores ownership afterward.
- Expanded validator checks to include actual active credentials and explicit timeout/permission fault injection, labeled as such.
- Replaced the previously exposed Vault bootstrap root token. Verified old-token rejection and replacement-token validity; preserved child sessions. Secret values were not printed.

Legacy rotation, failed recovery handling, incident closure validation, concurrent execution, interrupted-operation reconciliation, dynamic containment/recovery, and identity isolation pass in the full run.

## Remaining actions

1. **A01 clean installation and reproducibility.** Exercise a fresh isolated installation from declared source and dependency/image versions. The existing runtime is PostgreSQL 17 on Debian 13, while Dockerfile.base still targets Debian 12 and its default PostgreSQL package. Align the build with the persistent data layout and verify it without replacing the existing database with a different major version. The CI workflow currently runs regression/fixture checks rather than full live acceptance.
2. **A23 full lifecycle of the stack.** The runner currently proves a PostgreSQL restart with preserved service IDs. It does not yet prove full-stack down/up, the documented unseal sequence, and an isolated destructive reset that preserves unrelated Docker projects.
3. **A25 complete keyboard workflows.** Actual browser login, failed login, focus movement, and narrow/wide overflow checks pass. Intake, independent approval, incident actions, and evidence navigation still need full keyboard journeys. UI actions that invoke the host-oriented scenario runner also require separate verification now that runtime containers no longer receive host login credentials.
4. **Acceptance depth and implementation gaps.** Strengthen A19 from its placeholder historical fingerprint to an actual revoked version and independently compare issuer state. A21 currently uses a controlled interruption after the issuer effect; add a real worker-termination test. Provisioning remains synchronous rather than a durable background queue. Finish the requirement-to-evidence trace before declaring completion.
5. **Optional adapters.** X01–X05 remain documented live-integration checks and do not block core acceptance. They require configured live integration environments; GitLab and iTop may be local, so not every adapter requires SaaS. No adapter success is claimed. Previously running GitLab and runner connectivity were restored after the core checks, but GitLab readiness and integration behavior were not acceptance-tested. The temporary GitLab access-network attachment must be incorporated into its optional Compose configuration before the next recreation. Any previously exposed GitLab administrator credentials still need their own rotation and rejection check.

## Resume safely

Use a single worker. Do not rebuild/recreate PostgreSQL against a different major version on the existing volume. The preserved pre-change backup is `/Users/llody/.codex/backups/secret-exposure-poc-20260909-211855`. Current services use `poc-lab-base:latest`; host verification uses `/tmp/secret-exposure-check-venv/bin/python`. Run focused tests through `make unit-test` from `poc`, then run the scenario runner on the host. Do not mount host bootstrap root credentials or login-password files into ordinary services. Do not treat the three incomplete scenarios or optional adapter skips as passes. No commit or publication was performed.
