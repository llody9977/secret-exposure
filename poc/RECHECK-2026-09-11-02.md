# Verification update

Verdict: improved, still not acceptance-ready. No application source changed during verification.

Fresh full run `run_20260911_051103_88411962` reports **23 PASS, 3 FAIL (A21, A22, A26), 5 optional skips**. All **32 regression tests pass**. The source snapshot stayed unchanged during the run. [Report](evidence/run_20260911_051103_88411962/report.md).

## Confirmed improvements

- Authenticated Evidence Hub now opens valid JSON in a real browser.
- Identity demo now returns HTTP 200/status ok; it no longer requires the host password file.
- Repeating static reconciliation returned the same version and changed=false on the repeat.
- Normal rotation now distinguishes effects performed in the current attempt from reconciliation.

## Current failures and actions

1. **A21/A22 restart regression.** The A21 command uses Compose up for control-api without --no-deps. In this run it recreated PostgreSQL and Vault during the restart (container creation timestamps fall inside the A21 run). Vault was sealed; operation resume failed with RuntimeError and the following workload boundary probe failed. A23 later explicitly unsealed Vault. A direct repeat of the cross-service probe then correctly returned 403. Restart only the stopped worker, e.g. docker start on its exact container, or explicitly preserve dependencies and wait for readiness. The real in-flight mutation/commit crash window still remains untested because the kill follows a completed simulated-interruption request.
2. **A26 name collision.** scripts/run_scenarios.py uses container() as a helper and container as a local loop variable. Python cannot resolve the helper in the initial comprehension; the scenario fails before any offline checks. Rename the loop variable and rerun actual network checks.
3. **False Evidence Hub error.** openEvidenceHub() calls window.open with noopener and treats its null return as proof of blocking. In the real browser the JSON tab opened successfully while the original page showed Browser blocked the evidence tab. Use a download or an opening strategy that does not mistake noopener behavior for failure.
4. **A01/A23 acceptance remains overstated.** A01 does not perform a clean installation. A23 inspects Makefile text rather than executing an isolated reset and checking unaffected resources. Keep them incomplete until these exercises exist.
5. **A25 still does not prove keyboard reachability or persisted approval.** Its helpers programmatically focus controls, bypassing tab order. It closes the approval modal before asserting the approval button is invisible, which also happens when approval fails. Assert the specific intake response and persisted approval state before closing; exercise full narrow and wide keyboard workflows and incident actions.

The legacy service was unhealthy after interrupted rotation. Once Vault was unsealed, the same operation op-68ace2f0 was explicitly resumed and returned completed/verified/healthy; legacy health returned 200. These recovery checks do not replace the failed acceptance run.

X01–X05 remain legitimate optional live-integration skips, not core failures. Full live acceptance is still absent from CI. Other demo variants need verification because the runner still has host-only dependencies such as Docker and .env.
