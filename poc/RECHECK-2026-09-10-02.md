# Independent recheck

Verdict: improved, but not fully resolved. Current runner status is 26 PASS, zero FAIL, zero INCOMPLETE, and five optional skips. This is not sufficient evidence of full contractual acceptance.

## Current verification

Run `run_20260910_134156_0d0e8dcf` completed successfully with 60 correlated events and an unchanged source snapshot during execution. All 32 regression tests passed. See [run report](evidence/run_20260910_134156_0d0e8dcf/report.md) and [assertions](evidence/run_20260910_134156_0d0e8dcf/assertions.json). No application source was changed during this review.

The Debian base declaration now uses trixie, the optional GitLab service has a persistent access-net attachment, the intake UI supports independent submission and approval, and A19 now attempts a real historical fingerprint lookup and compares issuer rotation state. These are improvements over the previous checkpoint.

## Findings and required actions

1. **High: clean-install and reset checks still produce unsupported passes.** A01 (`scripts/run_scenarios.py:155`) checks source strings, host CPU count, and an existing health endpoint. It never installs a fresh stack. Its PostgreSQL-version assertion only searches for the word postgresql. A23 (`scripts/run_scenarios.py:1180`) restarts PostgreSQL, observes already-unsealed Vault, and searches Makefile text for reset arguments. It never performs full-stack down/up or an isolated reset. Restore incomplete status until real fresh-install and reset exercises run, with before/after inventories proving unrelated projects survive. The base tag and installed packages also remain unpinned, and CI still omits live core acceptance.

2. **High: A21 does not kill the operation worker.** `scripts/run_scenarios.py:1073` starts an unrelated Python sleep process and terminates it. The actual operation still uses the simulated-interruption branch. This demonstrates controlled reconciliation but does not validate the real crash window, lock release, or restart of the process performing the issuer mutation. Kill that actual worker after observing the external side effect, then verify recovery and exactly one issuer transition.

3. **Medium: Evidence Hub is broken despite A25 passing it.** The navigation is a plain link to `/api/evidence` (`control/static/index.html:534`), whereas authentication uses bearer tokens attached by the fetch wrapper. Direct navigation returns HTTP 401, confirmed live. A25 merely compares the link href (`scripts/run_scenarios.py:1330`). Use authenticated retrieval and an evidence view/download, then actually activate the link and verify the result in the browser.

4. **Medium: the identity demo fails under the restricted runtime.** An authenticated POST to `/api/demo/identity` returned HTTP 200 with application status error; output identified missing `auth_credentials.json`. The endpoint (`control/main.py:1043`) launches the host-oriented runner inside the control container. Keep password files isolated; implement a scoped server-side demo execution path or an explicitly host-run interface. Test the other demo endpoints, which use the same runner pattern.

5. **Medium: A25 overstates keyboard and approval coverage.** The new journey uses click() and fill(), not keyboard traversal, and runs the full flow only at desktop width. After clicking Approve, it waits one second and records True without verifying the response or persisted state (`scripts/run_scenarios.py:1314`). Incident checking proves only that a table is visible. Exercise keyboard-only intake, independent approval, incident state/actions, and evidence navigation at both widths; assert actual state transitions and visible failures.

Additional gaps remain: A19 must remove its hardcoded fallback fingerprint and assert exact historical-version correlation; synchronous provisioning is not a durable execution queue. Do not describe the updated 26/26 count as closing these requirements.

X01–X05 remain legitimate optional integration skips and are not included in the findings above. They require live configured integration environments; GitLab and iTop can be local, so not all require SaaS.
